# 如何收录

本仓库只维护**能落地的开源 Git 仓库**，不是论文索引，也不是闭源产品黄页。

## 收录标准

同时满足越多越好：

1. 有可运行代码或可下载权重，而不是只有宣传页。
2. 和视频生成、音频生成、漫画 / 故事可视化、角色一致性、或上述管线工具直接相关。
3. 许可清晰；商用限制要在 README 里写明。
4. 仍在维护，或虽停更但至今仍是该方向的事实标准。
5. 对本工作区的动态漫画、讲解视频、配音管线有实际参考价值。

不收：

- 没有源码的 SaaS / App 商店链接
- 纯镜像、空壳 fork、星标农场
- 无法核验的「神级整合包」

## 提交方式

在对应分类的表格里加一行，保持现有列：

`项目 | 一句话 | 许可`

- 项目列用官方仓库链接，组织 / 仓库名以 GitHub 当前地址为准。
- 一句话不超过 40 字，写「能做什么」，不要写营销词。
- 许可填 SPDX（如 `Apache-2.0`、`MIT`）。官方选 `Other` 或未声明时写 `见仓库`。
- 新分类先想清楚它是否真的独立；能并进现有节就不要新开一节。

改完后更新 README 顶部的「最近核对」日期。
